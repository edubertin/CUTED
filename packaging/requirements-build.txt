# Versoes espelhadas do ambiente de desenvolvimento validado em 2026-06-10
# (runtime Codex, Python 3.12.13). Torch CPU-only via indice dedicado.
--extra-index-url https://download.pytorch.org/whl/cpu

# +cpu garante a wheel do indice CPU; se a versao nao existir la, o build deve
# falhar em vez de cair silenciosamente na wheel CUDA do PyPI.
torch==2.12.0+cpu
ultralytics==8.4.62
lap==0.5.13
opencv-python-headless==4.13.0.92
yt-dlp==2026.7.4
faster-whisper==1.2.1
imageio-ffmpeg==0.6.0
numpy==2.3.5
pillow==12.2.0
pywebview==5.4

pyinstaller>=6.21.0,<7
